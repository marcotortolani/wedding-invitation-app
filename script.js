const body = document.querySelector("body");

const rings = document.querySelector(".rings");

if (body.className === "inicio") {
    
    

    // setInterval(() => {
    //     rings.classList.toggle("scale");

    // }, 2000);
}

if (body.className === "date") {

    // setInterval(() => {
    //     rings.classList.toggle("spin");
    // }, 2000);

}